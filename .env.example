DATABASE_URL=
OPENAI_API_KEY=
# Optional override for /api/score (defaults to gpt-4o)
OPENAI_SCORE_MODEL=
# Canonical site URL for production (metadata, OG). Must include https://
NEXT_PUBLIC_SITE_URL=
# Optional: Google Analytics 4 measurement ID. If unset, no analytics scripts load.
NEXT_PUBLIC_GA_ID=
# Optional: external application portal (e.g. FACTS). If unset, Apply uses /admissions.
NEXT_PUBLIC_APPLY_URL=
